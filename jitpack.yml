jdk:
  - openjdk21
before_install:
  - sdk install gradle 8.10.2
  - sdk use gradle 8.10.2
install:
  - gradle clean build publishToMavenLocal
  - cp -r ./* /tmp
  - cd /tmp && gradle clean build publishToMavenLocal